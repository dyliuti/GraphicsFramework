#ifndef MAINDEFINE_H
#define MAINDEFINE_H
#include <set>

enum class ToolType {
    Cursor, // 仅是鼠标移动
    Select,
    Pen,
    Eraser,
    Rect,
    Star,
    Cirle,
    Ellipse,
};

#endif // MAINDEFINE_H
